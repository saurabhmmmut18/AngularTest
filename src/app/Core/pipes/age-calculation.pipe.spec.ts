import { AgeCalculationPipe } from './age-calculation.pipe';

describe('AgeCalculationPipe', () => {
  it('create an instance', () => {
    const pipe = new AgeCalculationPipe();
    expect(pipe).toBeTruthy();
  });
});
